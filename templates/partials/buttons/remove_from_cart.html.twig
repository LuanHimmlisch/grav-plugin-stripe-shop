{#
This partial is the most basic setup for a remove from cart button that the plugin requires.
Simply create an <a> or any other element that you want,
with the data-remove-from-cart="insert your stripe sku here"

Example: add this to your templates:
{% include 'partials/buttons/remove_from_cart.html.twig' with {sku: page.header.sku} %}
#}

<button class="shop_button_remove" data-remove-from-cart="{{ price_id }}">
    <i class="fa fa-trash" style="pointer-events: none;"></i>
</button>

{# <a href="#" data-remove-from-cart="{{ sku }}">Remove from Cart</a> #}